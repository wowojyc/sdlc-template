# 审查说明（REVIEW.md）

> **谁写**：技术负责人写一次，**每月调优**（给发现项打分、调 Nit 上限、排除噪声源）。
> **放哪**：仓库**根目录**。
> **怎么触发**：它**不负责触发**——由托管 Code Review 服务或 CI 里的 `claude-code-action` 在开 PR 时读取并按它审。
> **关键**：它是**人写给 AI 的"尺子"**，不是 AI 审完生成的报告。发现项贴在 PR 评论里，**本身不阻塞合并**。

## 审查轮次（Passes）
跑三遍，每条发现贴轮次标签：
- **Bugs**：逻辑错误、没兜住的边界、隐蔽回归
- **Security**：注入风险、鉴权缺口、日志泄露个人信息
- **Compliance**：改动是否对得上 `spec.md` / `plan.md` / 我们的设计原则

## 什么算 Important
会**破坏行为 / 泄露数据 / 违反策略**的标 Important；
风格、命名这类算 **Nit**。

## 收敛 Nit
每次最多报 **5 条** Nit，其余用"还有 N 条"概括。

## 测试改动审查
- **新增覆盖**：行为变更后补的用例（TDD 跟进）→ 正常
- **放宽断言**：删/改已有断言迁就实现 → 标 **Important**（作弊信号：测试失去“行为契约”意义）
- 判断标准（白名单 / 黑名单 / 自查三问）见 README「测试文件改动规则」

## 不报这些
- 生成文件目录（如 `src/gen/`）
- CI 已经强制的项（lint、格式化等）

---
<!-- 填写提示（填完可删）
· 触发二选一：
  1) 托管 Code Review 服务：管理员启用 + 勾选仓库，之后每次开 PR 自动跑（最快）。
  2) CI 里的 claude-code-action：要自管流水线、或模型走 AWS Bedrock / Vertex / Foundry 时用。
· 想"有 Important 就不让合并"：去读 check run 的机器可读严重度计数，自己配门禁。
· 批准永远来自人（分支保护 + code owner）；写代码的 agent 无从批准自己的代码。
· @claude 二次触发：PR 评论里 @claude，它会按发现项自己改并推送；可包成斜杠命令"修到全绿等人批"。
-->
